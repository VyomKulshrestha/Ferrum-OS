[package]
name = "ferrum-neural-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Fail-closed neural intent contracts for FerrumOS"

[lib]
path = "src/lib.rs"

[dependencies]
hmac = { version = "0.12.1", default-features = false }
sha2 = { version = "0.10.8", default-features = false }

